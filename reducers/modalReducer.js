import { CLOSE_MODEL, DELETE_MODEL } from "../action/types";
const initialState = {
  close: false
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
